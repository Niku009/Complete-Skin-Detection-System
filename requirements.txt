# Skinwise production dependencies.
# Target: Streamlit Cloud, Python 3.11 (see runtime.txt).
#
# Key design decision: we install tf-keras (the standalone Keras-2 backport)
# alongside tensorflow==2.15.1. This means src/tf_compat.py ALWAYS resolves
# to tf_keras on both Python 3.11 (Cloud) and Python 3.12+ (local), keeping
# behavior identical and immune to Keras-3 being present in the environment.

# --- Core UI ---
streamlit==1.36.0

# --- Imaging ---
pillow==10.3.0
opencv-python-headless==4.9.0.80
numpy==1.26.4
h5py==3.10.0

# --- Object detection (PyTorch / Ultralytics) ---
ultralytics==8.3.27
torch==2.2.2
torchvision==0.17.2

# --- Classifier stack (PyTorch + timm) ---
timm==1.0.7
albumentations==1.4.10

# --- TensorFlow + Keras-2 backport + Keras-CV (acne) + skin type ---
tensorflow==2.15.1
tf-keras==2.15.0
keras-cv==0.9.0

# --- Auto-download of model weights on first launch ---
gdown==5.2.0
