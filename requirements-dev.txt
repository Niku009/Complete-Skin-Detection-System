# Local dev requirements for Python 3.12 / 3.13 where TF 2.15 has no wheels.
# Same library set as production, but with versions compatible with the
# newer Python and the tf-keras Keras-2 backport (needed because TF 2.16+
# ships Keras 3 by default).

streamlit==1.36.0
pillow==10.3.0
numpy==1.26.4
h5py==3.10.0

ultralytics==8.3.27
torch==2.2.2
torchvision==0.17.2

timm==1.0.7
albumentations==1.4.10

tensorflow==2.16.2
tf-keras==2.16.0
keras-cv==0.9.0

gdown==5.2.0
